// Entities Definition for `config.cumin`

type Item = Html | Json;

struct Html {
  name: String,
  url: String,
  pattern: String,
  format: String = "{name}: {matched}",
  type: String = "html",
}

struct Json {
  name: String,
  url: String,
  pattern: String,
  format: String = "{name}: {matched}",
  type: String = "json",
}
